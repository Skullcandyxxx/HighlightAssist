
fastapi
uvicorn[standard]
websockets
jinja2  # Required for FastAPI templating (web dashboard)
pystray
Pillow
plyer
psutil  # For process monitoring and health checks
# Optional notification backends for native notifications
win10toast; platform_system == "Windows"
notify2; platform_system == "Linux"
pync; platform_system == "Darwin"
